# Optional. Without this value, the prototype uses its deterministic local composer.
OPENAI_API_KEY=

# Keep model selection configurable so it can be evaluated before competition use.
OPENAI_MODEL=gpt-5.4-nano
